# Runtime dependencies only (no dev tools).
# Single source of truth - backend/Dockerfile installs via `pip install -r`.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic[email]>=2.6,<3.0
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0
# bcrypt pinned <4.1: passlib 1.7.x + bcrypt 4.1+ raises a misleading
# "password cannot be longer than 72 bytes" error on any hash call
# (upstream incompat; see https://github.com/pyca/bcrypt/issues/684).
bcrypt>=4.0,<4.1
python-multipart>=0.0.9
pytest>=7.0,<9.0
pydantic-settings
httpx>=0.24.0
email-validator>=2.0.0
